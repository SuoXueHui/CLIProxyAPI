# Managed by scripts/cpa-router/reload-backend.sh.
upstream cpa_proxy_backend {
    server cpa-blue:8317;
    keepalive 64;
}

upstream cpa_management_backend {
    server cpa-blue:8317;
    keepalive 64;
}
